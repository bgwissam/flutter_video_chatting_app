import 'package:connectycube_sdk/connectycube_calls.dart';
import 'package:flutter/material.dart';

class SelectedOponentScreen extends StatelessWidget {
  final CubeUser user;

  const SelectedOponentScreen({Key key, this.user}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
